# server/requirements.txt

# --- Core Web Framework ---
# Why: Used for the API and routing (app.py)
Flask
Flask-CORS

# --- Data Science & AI ---
# Why: Used for data manipulation in generate_data.py and services.py
pandas

# Why: The core ML model used for prediction (train_model.py)
xgboost

# Why: Often required for data preprocessing or pipeline support alongside XGBoost
scikit-learn

# --- Testing ---
# Why: Required to run the unit tests in the CI pipeline
pytest